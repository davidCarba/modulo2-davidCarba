/*
 * Aquí podéis hacer los ejercicios y
 * practicar Javascript!
 */

/*crear objeto con new 2 parametros una funcion saludar y otra a quien saluda */

/*function Usuario(message, name) {
  this.greet = message;
  this.user = name;
  return this.greet, this.user;
}

Usuario("hola", "manuel");
console.log(this.greet + " " + this.user);*/

/*function Usuario(message, name) {
  this.greet = message;
  this.user = name;
  let nombre = this.user;
  for (i = 0; i < nombre.length; i++) {
    if (nombre.charAt(i) == "a" || nombre.charAt(i) == "A") {
      console.log(this.greet + " " + this.user);
    }
  }
}

Usuario("hola", "manuel");*/

/*function saludo() {
  alert("Welcome " + this.name);
}

function user(func, name) {
  this.name = name;
  this.sayHi = func;
}

let user1 = new user(saludo,"name");

user1.sayHi();*/
